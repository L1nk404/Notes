
`shift + v` copies select all the line
`y` yanke it
`f<char>`select the first occurence of the <char>
`w` select next word
`cw` change that word
`=ap` ident

## Telescope
`leader + ff` find files
`leader + fd` live grep
`leader + fb` find buffer
`leader + fh` help tag

## mason
type: Ḿason -> Opens interface

`g+?` opens shortcut help
